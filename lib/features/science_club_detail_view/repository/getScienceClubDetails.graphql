query GetScienceClubDetails($id: ID!) {
  Scientific_Circles_by_id(id: $id) {
    name
    description
    source
    cover {
      filename_disk
    }
    links {
      name
      link
    }
    logo {
      filename_disk
    }
    department {
      name
    }
  }
}
