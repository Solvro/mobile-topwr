query GetScientificCircleDetails($id: ID!) {
  Scientific_Circles_by_id(id: $id) {
    name
    description
    cover
    links {
      name
      link
    }
    logo
    department {
      name
    }
  }
}
