query GetScienceClubs {
  Scientific_Circles(limit: 300) {
    id
    name
    department {
      id
      name
      code
      betterCode
    }
    tags {
      Tags_id {
        id
        name
      }
    }
    logo {
      filename_disk
    }
    source
    shortDescription
    type
  }
}
